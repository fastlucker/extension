// import { DEVICE_HEIGHT, DEVICE_WIDTH } from '@modules/common/styles/spacings'
const DEVICE_WIDTH = 600
const DEVICE_HEIGHT = 923

document.getElementById('root').style.width = `${DEVICE_WIDTH}px`
document.getElementById('root').style.maxWidth = `${DEVICE_WIDTH}px`
document.getElementById('root').style.height = `${DEVICE_HEIGHT}px`
document.getElementById('root').style.maxHeight = `${DEVICE_HEIGHT}px`

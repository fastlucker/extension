import React from 'react'
import RNFastImage, { FastImageProps } from 'react-native-fast-image'

const FastImage: React.ComponentType<FastImageProps> = (props) => <RNFastImage {...props} />

export default FastImage

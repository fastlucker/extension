import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  accItemStyle: {
    flexDirection: 'row',
    alignItems: 'center'
  },
  balanceContainer: {
    alignItems: 'center'
  }
})

export default styles

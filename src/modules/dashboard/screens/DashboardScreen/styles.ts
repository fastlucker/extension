import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 16,
  },
  accItemStyle: {
    flexDirection: 'row',
    alignItems: 'center',
  },
})

export default styles

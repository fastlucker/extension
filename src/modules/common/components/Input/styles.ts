import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  input: {
    height: 40,
    borderBottomWidth: 1,
    padding: 12,
    marginBottom: 16,
  },
})

export default styles

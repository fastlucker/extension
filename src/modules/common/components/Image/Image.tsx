import React from 'react'
import FastImage, { FastImageProps } from 'react-native-fast-image'

const Image: React.ComponentType<FastImageProps> = (props) => <FastImage {...props} />

export default Image
